import NewsArticle from 'views/NewsArticle';

export default function NewsArticlePage(): JSX.Element {
	// const router = useRouter();
	// if (!router.isFallback && !slug) {
	// 	return <ErrorPage statusCode={404} />;
	// }

	return <NewsArticle />;
}
